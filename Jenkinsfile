pipeline {
  agent any
  stages {
    stage('') {
      steps {
        echo 'Iniciando compilación'
        sh 'mvn clean install'
        echo 'Compilación finalizada'
      }
    }

  }
  environment {
    mensaje = ''
  }
}